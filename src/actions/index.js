import { openSnackbar, closeSnackbar } from "./snackbar";
import { changeColor, changeFont } from "./navigationChange";
import { syncUserInfo, syncPageInfo, syncOtherPageInfo } from "./syncPage";

export { openSnackbar, closeSnackbar, 
    changeColor, changeFont,
    syncUserInfo, syncPageInfo, syncOtherPageInfo
 };
